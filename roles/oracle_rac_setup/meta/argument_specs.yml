---
argument_specs:
  main:
    short_description: "Deploy Oracle RAC cluster"
    options:
      oracle_rac_setup_host:
        description: Target host address
        type: str
        required: true
      oracle_rac_setup_username:
        description: Authentication username
        type: str
      oracle_rac_setup_password:
        description: Authentication password
        type: str
        no_log: true
      oracle_rac_setup_api_key:
        description: API key for authentication
        type: str
        no_log: true
      oracle_rac_setup_validate_certs:
        description: Validate SSL certificates
        type: bool
        default: true
      oracle_rac_setup_state:
        description: Desired state
        type: str
        default: present
        choices: [present, absent]
