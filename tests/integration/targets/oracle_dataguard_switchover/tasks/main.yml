---
- name: Test oracle_dataguard_switchover module
  hosts: localhost
  gather_facts: false
  tasks:
    - name: Create dg_switchover (check mode)
      stevefulme1.oracledb.oracle_dataguard_switchover:
        host: "{{ test_host }}"
        name: "test-dg-switchover"
        state: present
      check_mode: true
      register: check_result
      ignore_errors: true

    - name: Verify check mode reports changed
      ansible.builtin.assert:
        that:
          - check_result is changed or check_result is failed
        fail_msg: "Check mode did not report changed"

    - name: List dg_switchovers
      stevefulme1.oracledb.oracle_dataguard_switchover_info:
        host: "{{ test_host }}"
      register: list_result
      ignore_errors: true

    - name: Verify list returns a list
      ansible.builtin.assert:
        that:
          - list_result.dg_switchovers is defined or list_result is failed
        fail_msg: "List did not return expected structure"
